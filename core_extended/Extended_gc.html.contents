 
      <pre><span><span class="keyword">val</span> without_compactions : <code class="type">?logger:(string -&gt; unit) -&gt; f:('a -&gt; 'b) -&gt; 'a -&gt; 'b</code></span></pre>
      <div class="info"><code class="code">without_compactions f a</code> will call f a so that Gc.compact is never called
    during its execution, then restore compactions to the previous setting.</div>
    
